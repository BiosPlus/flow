[module]
  [module.hugoVersion]
    min = "0.165.0"

[markup]
  [markup.highlight]
    noClasses = false
    lineNos = false
    tabWidth = 2
    wrapperClass = "highlight"
  [markup.goldmark.renderer]
    unsafe = false
  [markup.tableOfContents]
    startLevel = 2
    endLevel = 3
