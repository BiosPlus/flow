baseURL = "/"
locale = "en-au"
title = "Your site title"
publishDir = "build"

themesDir = "theme"
theme = "flow"

[taxonomies]
  tag = "tags"

[params]
  description = "A short line about the site."
  pinnedTags = ["thought", "tip"]
